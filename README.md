## MY FIRST CODE ON CS50P COURSE, HARVARD.
- Side effects is displayed in terminal 

# How to run it.
call `python` or (`py`) in terminal then the file name to provide side effect(run/output) I.E `py hello.py`.
`hello.py is my file name.`
- I used offline VS code

- I followed Prof David Malan, promptly with his step by step examples, which would be found on the VS code folder.
- For grounded understanding, I imagined examples then code it down which gave me side effect i expected. I used "analogy" comments to identify my examples"

# CS50P - Lesson 0: Functions and Variables

This is my repository for tracking my Python learning journey with Harvard University's CS50p course, taught by Professor David Malan.

## What I Learned in Lesson 0, Day 0:
* **python/py:** Computer only understands binary i.e. 0s and 1s, so the "py" called in terminal will translate to computer in the computer language 0s & 1s then translate the output to human language. 

* **String:** a string is a sequence of characters used to represent text. Anything inside single quotes ('...'), double quotes ("..."), or triple quotes ("""...""") is considered a string. Learned how running a program in the terminal using `py hello.py` provides the output called "side effect".

* **Side Effects:** In programming, a "side effect" is an observable change that happens when a line of code runs. For example, using `print()` causes the side effect of displaying text in the terminal window. It is the result of your code that shows in the terminal

* **Function:** functions are reusable code. Functions are actions or verbs that the computer or computer language already know how to use which aids we humans to use the function codes to execute from the computer. i.e. *print*

* **Arguments:** are Information that passes through functions to display as side effects. it somehow influences functions i.e. *print as in **function***, *`("hello,world")` as in argument* `print("hello, world.")`
*hint:* the parentheses and the double quote made it possible for argument to display exactly what's in it.

* **Variables:** variables have the ability to store information in the computer memories. Practiced using the `input()` function to capture variables (like names, ages, and weights) and `print()` to output them.
*hint:* To use "print" to output the information stored, the double quotes will be excluded.

* **String Formatting:** Tested spacing using both the `+` concatenation operator and commas `,` for automatic spacing, another argument wants to be added.

## My Personal Code Analogies:
- For a grounded understanding, I, Imagined real-world examples and code them down. In the lessons `hello.py` and `L0D1`, I built custom prompts to print out a user's name, age, and weight asked previously. `(L0D1 means Lesson0 Day1)`

`I asked AI for complex tasks to give me, in order to solve them.`

*****************************************************************************************

## L0D2
- One of the types of data in python is called `string(str)`, in python string is a sequence of program.
- It appears that `print funtion` automatically made the information appeared on the next line.
- In the documentation like `print(* objects, sep='', end='\n', file=sys.stdout, flush=False){this is the official documentation for print function}` evenything inside the parenthesis are potencial argument to the function.
- When we are looking at arguments in the documentation there's is a term it is being used, called `parameters` to a function.when we are talking about what we can ask to a functions and what those input are called those are parameters
- when the function is used and parse in values inside those parentheses those input/values are `argument`.
## we are looking at what the function can take versus what you are actually pasing to the function.


## Note 
- One of the easiest way to learn programing language is to learn how to read the documentation.All of the answers to your questions might be there even if it is not obvious at first.

## Explanation of `print(* objects, sep='', end='\n', file=sys.stdout, flush=False)
- `* objects:` can take any form, number and type of object, i.e. zero string, one string, two strings, technically infinity strings.
- `sep=''` parameter: the short form of separation, by default uses it to override(block or remove) space
-  `end='\n'`: the backslach and N means new-line, by default this print function will end every line of code by using `/n`.

